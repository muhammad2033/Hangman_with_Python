# step 2
import random
word_list = ["ardvark","baboon","camel"]
chosen_word = random.choice(word_list)
# print(chosen_word)

# testing code
print(f"pssst , the solution is : {chosen_word}")

# TODO-1- create an empty list called display.
# for each letter in the chosen_word , add a '_' to 'display'

# so if the chosen_word was "baboon ", display should be
#  ['_','_','_','_','_','_'] ,and a '_' to 'display'

display = []

word_length =len(chosen_word)
for position in range(word_length):
    display +="_"

print(display)

#  todo-2 loop through each position in the chosen_word;
# if the letter at the position matches 'guess 'then
# reveal that letter in the display at the position .
# e.g . if the user guessed 'b' and the chosen_word was
# 'baboon ', then display should be ['b','_','b','_','_','_']

guess =input("guess a letter:").lower()
# print(guess)

for position in range(word_length):
    letter = chosen_word[position]
    if letter == guess:
        display[position] = letter
# TODO-3- print 'display' and you should see the guessed 
# letter in the correct position and every other letter is 
# replaced with '_'

print(display)




 






























































































































































































































































































































































































































































































































































        