# step3                                                                                  
import random
word_list = ["ardvark","baboon","camel"]
chosen_word = random.choice(word_list)
# print(chosen_word)

# testing code
print(f"pssst , the solution is : {chosen_word}")

# create blanks
    
display = []
word_length =len(chosen_word)
for position in range(word_length):
    display +="_"

print(display)

# todo-1:- use a while loop to let the user guess again .the loop
# should only stop once the user has guessed all the letters in the chosen_word and 'display 
# has no more blanks ("_").then you can tell the user they have won
end_of_game = False

while not end_of_game:
    guess =input("guess a letter:").lower()

    # print(guess)

    # check guessed letter 

    for position in range(word_length):
        letter = chosen_word[position]
        print(f"current position:{position}\n current letter: {letter}\n guessed letter {guess }")
        if letter == guess:
            display[position] = letter

    print(display)

    if '_' not in display:
        end_of_game = True
        print("you win!")



 






























































































































































































































































































































































































































































































































































        